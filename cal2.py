import print

# Original file contents
# Return ONLY a JSON array. Each object must contain:
#   - `file_path`: relative path of the file to update
#   - `fixed_code`: the full, updated file contents (the whole file) as a single-line string using \n for newlines
#   - `explanation`: a short reason for the change

# Example response:
# [
#   {
#     "file_path": "relative/path.py",
#     "fixed_code": "line1\nline2\nline3",
#     "explanation": "Fix logic issue"
#   }
# ]
